{% extends 'layout.twig' %}

{% block title %}
    Error {{ code }}
{% endblock %}

{% block content %}
<h1>Error {{ code }}</h1>
<h2>{{ message }}</h2>
{% if trace %}
	<pre>
{{ message }} on {{ file }} ({{ line }})
{{ trace }}
	</pre>
{% endif %}
{% endblock %}